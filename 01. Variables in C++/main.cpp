/* ==========================================================
   TOPIC: Variables in C++
   - Definition: A labeled storage box To store data.
   - Memory: Occupies physical RAM (volatile memory).
   - Storage Locations: Stack or Heap.
   - Naming Rules: Must start with a letter/underscore, contain letters/numbers/underscore, case-sensitive, no keywords.
   ========================================================== */

#include <iostream>

using namespace std;

int main(){
    int a; //declare
    a=5; // Assign
    int b = 5; //declare and Assign
    
    int c, d, e; //Multiple variable declaration
}